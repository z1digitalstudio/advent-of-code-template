const parseInput = (rawInput) => rawInput;

export const part1 = (rawInput: unknown) => {
  const input = parseInput(rawInput);

  return;
};

export const part2 = (rawInput: unknown) => {
  const input = parseInput(rawInput);

  return;
};
